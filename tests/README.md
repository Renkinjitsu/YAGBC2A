The tests in the current directory are made to work with the `Tester` tool (`[project-root]/tools/Tester.py`).

The test-implementation-convention is described and explained at the tool's documentation.
